<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="AppTheme.NoTitleBar" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <style name="AppTheme.NoTitleBar.Fullscreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <!-- Custom theme. -->
    <style name="Theme.tobacco" parent="android:Theme.Light.NoTitleBar">
    	<item name="android:windowBackground">@drawable/bitmap</item>
    	<item name="android:textSize">20sp</item>
    	<item name="android:textColor">#000000</item>
    </style>
    
    <style name="Theme.application" parent="android:Theme.NoTitleBar.Fullscreen">
    //可以加快绘制速度（和ListView不兼容）
    	<item name="android:windowBackground">@null</item>
    </style>
    
    <style name="Theme_Translucent">
    //背景半透明
    	<item name="android:windowBackground">@color/translucent</item>
    	<item name="android:windowNoTitle">true</item>
    	<item name="android:windowIsTranslucent">true</item>
    	<item name="android:windowAnimationStyle">@+android:style/Animation.Translucent</item>
    </style>
    
    <style name="Theme_Dialog" parent="android:style/Theme.Dialog">
    //自定义对话框
    	<item name="android:windowFrame">@null</item>									Dialog的windowFrame框为无
    	<item name="android:windowIsFloating">true</item>                           	是否浮现在activity之上
    	<item name="android:windowIsTranslucent">false</item>							是否半透明
    	<item name="android:windowNoTitle">true</item>									是否显示标题
    	<item name="android:windowBackground">@android:color/transparent</item>			设置dialog的背景
    	<item name="android:backgroundDimEnabled">false</item>							背景是否模糊显示
    </style>
    
    <style name="Theme.setting" parent="AppTheme">
    //设置界面主题
    <!-- 
        <item name="android:windowNoTitle">false</item>									显示窗体标题栏
        <item name="android:windowTitleSize">42.0dip</item>								窗体标题栏高度
        <item name="android:windowTitleStyle">@style/setting_title</item> 				窗体标题栏样式
        <item name="android:windowTitleBackgroundStyle">@style/setting_title_bg</item>	窗体标题背景风格
        <item name="android:textColorPrimaryInverse">#ff0000</item>                     preference标题文本颜色
        <item name="android:textColorPrimary">#000080</item>                            preference一级文本颜色
        <item name="android:textColorSecondary">#008080</item>                          preference二级文本颜色
        
        <item name="android:listViewStyle">@style/setting_listview</item>				preference就是用列表实现的,这里设置其样式
        	还可以设置其它控件样式
        <item name="android:checkboxStyle"></item>
        <item name="android:autoCompleteTextViewStyle"></item>
        <item name="android:editTextStyle"></item>
        <item name="android:expandableListViewStyle"></item>
        <item name="android:radioButtonStyle"></item>
     -->
    </style>
</resources>